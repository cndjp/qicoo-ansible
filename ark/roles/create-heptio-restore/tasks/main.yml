- name: "Create Ark Restore"
  shell: |
     ark restore create --from-backup {{ ARK.BACKUP_NAME }} --include-namespace production,stagement
  register: result

- debug: var=result.stdout_lines
  when: result | success
  tags:
    - always
